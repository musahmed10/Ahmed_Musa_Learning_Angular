import { AvailabilityColorsPipe } from './availability-colors.pipe';

describe('AvailabilityColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailabilityColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
