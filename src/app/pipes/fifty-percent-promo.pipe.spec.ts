import { FiftyPercentPromoPipe } from './fifty-percent-promo.pipe';

describe('FiftyPercentPromoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiftyPercentPromoPipe();
    expect(pipe).toBeTruthy();
  });
});
